info:
  name: list
  type: http
  seq: 1
http:
  method: GET
  url: "{{baseUrl}}/pets"
  params:
    - name: limit
      value: "20"
      type: query
      disabled: true
    - name: offset
      value: "0"
      type: query
      disabled: true
    - name: status
      value: available
      type: query
      disabled: true
  auth:
    type: bearer
    token: "{{token}}"
docs: List all pets with optional filtering
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
