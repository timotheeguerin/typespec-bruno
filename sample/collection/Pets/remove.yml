info:
  name: remove
  type: http
  seq: 5
http:
  method: DELETE
  url: "{{baseUrl}}/pets/:petId"
  params:
    - name: petId
      value: "3"
      type: path
docs: Delete a pet
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "204"
