info:
  name: update
  type: http
  seq: 4
http:
  method: PUT
  url: "{{baseUrl}}/pets/:petId"
  params:
    - name: petId
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "name": "Buddy Jr."
      }
docs: Update a pet
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"
