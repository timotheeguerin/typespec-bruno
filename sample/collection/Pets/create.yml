info:
  name: create
  type: http
  seq: 2
http:
  method: POST
  url: "{{baseUrl}}/pets"
  body:
    type: json
    data: |-
      {
        "name": "Buddy",
        "tag": "golden-retriever"
      }
docs: Create a new pet
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "201"
