info:
  name: read
  type: http
  seq: 3
http:
  method: GET
  url: "{{baseUrl}}/pets/:petId"
  params:
    - name: petId
      value: "1"
      type: path
docs: Get a pet by ID
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"
