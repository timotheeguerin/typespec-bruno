info:
  name: read
  type: http
  seq: 2
http:
  method: GET
  url: "{{baseUrl}}/owners/:ownerId"
  params:
    - name: ownerId
      value: "7"
      type: path
  auth:
    type: bearer
    token: "{{token}}"
docs: Get owner by ID
