info:
  name: list
  type: http
  seq: 1
http:
  method: GET
  url: "{{baseUrl}}/owners"
docs: List all pet owners
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"
