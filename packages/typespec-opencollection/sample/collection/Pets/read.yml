info:
  name: read
  type: http
  seq: 3
http:
  method: GET
  url: "{{baseUrl}}/pets/:petId"
  params:
    - name: petId
      value: "42"
      type: path
  auth:
    type: bearer
    token: "{{token}}"
docs: Get a pet by ID
